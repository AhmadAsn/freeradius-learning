# =============================================================================
# proxy.conf — RADIUS Proxy & Federation (disabled by default)
# =============================================================================
# Enable proxy_requests = yes in radiusd.conf and $INCLUDE this file.
# =============================================================================

proxy server {
    default_fallback = no
}

realm LOCAL {
}

realm NULL {
}

realm DEFAULT {
}
